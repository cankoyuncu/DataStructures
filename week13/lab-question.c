#include <stdio.h>
#include <time.h>

srand(time(NULL));
data = rand

void addFront(struct node *start_ref){
    int data


}

void printfList(struct node *start_ref){
    struct node *temp = head;

    while(temp!=NULL){
        printf("%d", temp->data);
        temp = temp->next;
    }
}

void bubbleSort(struct node *head){
    struct node *ptr1;
    struct node *ptr2 = NULL;

    while(ptr1->next != ptr2){
        if(ptr->data > ptr1->next->data){
            swap(ptr1,ptr1->next);
        }
    }
}

void swap(struct node *a, struct node *b){
    


    
}