//Insterting an element in front of a list

#include <stdio.h>

struct node *addfront (struct node *head, int x){
	struct node *temp = (struct node *)malloc(size of (Struct node)){
	
	temp->data=x;
	temp->next=head;
	head->temp;
	return head;
	
};
